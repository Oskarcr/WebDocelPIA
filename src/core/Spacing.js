const Padding = {
    /**
     * El `padding` mas pequeño de la aplicacion
     * @readonly
     */
    SM: "var(--spacing-small)",

    /**
     * El `padding` que es "clave" <-- XD.
     * @readonly
     */
    MD: "var(--spacing-medium)",

    /**
     * No se porque tengo que indicar que es un
     * `padding` largo.
     * @readonly
     */
    LG: "var(--spacing-long)"
};

Object.freeze(Padding); 

export default Padding;